<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <script src="https://cdn.tailwindcss.com"></script>
   <title>Home</title>
   <link href="/static/style.css" rel="stylesheet">
   <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>


</head>


<body class="min-h-screen flex flex-col">
   {%include "components/navbar.html.jinja"%}
   <main class="flex-grow bg-[#0f2f4f]">

    <div class="max-w-6xl mx-auto px-6 py-10">

        <!-- Title -->
        <h2 class="text-2xl font-bold text-white mb-8">
            Search results for "{{ query }}"
        </h2>

        <!-- Results wrapper -->
        <div class="max-w-2xl">

            {% if results %}
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

                {% for products in results %}
                    <a href="/product/{{ products['ID'] }}"
                    class="flex items-center gap-6 bg-gray-100 border border-gray-300 rounded-3xl p-4 transition hover:shadow-lg hover:border-gray-400">

                        <!-- Image container -->
                        <div class="w-29 h-29 bg-white rounded-2xl flex items-center justify-center border border-gray-200">
                            <img src="{{ products['Image'] }}"
                                alt="{{ products['Name'] }}"
                                class="max-h-25 object-contain">
                        </div>

                        <!-- Text content -->
                        <div class="flex-1">
                            <h3 class="text-lg font-semibold text-gray-800">
                                {{ products["Name"] }}
                            </h3>

                            <p class="text-gray-700 mt-2 font-medium">
                                ${{ products["Price"] }}
                            </p>
                        </div>

                    </a>
                {% endfor %}

            </div>
            {% else %}
                <p class="text-gray-300">
                    No products found.
                </p>
            {% endif %}

        </div>

    </div>

</main>
   {%include "components/footer.html.jinja"%}
</body>
</html>
