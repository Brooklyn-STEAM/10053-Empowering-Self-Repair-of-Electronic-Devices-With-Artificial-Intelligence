<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Sign Up</title>
    <link href="/static/style.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>

<body>
{%include "components/navbar.html.jinja"%}


  <div class="flex items-center justify-center mt-9">
    <div class="bg-white w-full max-w-md p-8 border-1 shadow-2xl">
      <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">
        Create Account
      </h2>

        {% for message in get_flashed_messages()%}
          <p class="bg-red-300 w-full text-sm p-3 shadow-sm text-black font-bold rounded-lg">{{ message}} </p>
        {% endfor%}
        <br>
      <form method="post" class="flex flex-col gap-4">

        <div>
          <label class="block text-sm font-medium text-black mb-1">Full Name</label>
          <input
            required
            name="full_name"
            class="w-full px-4 py-2 border rounded-lg focus:ring-2 text-black focus:ring-indigo-500 focus:outline-none"
          >
        </div>

        <div>
          <label class="block text-sm font-medium text-black mb-1">Email</label>
          <input
            required
            type="email"
            name="email"
            class="w-full px-4 py-2 border rounded-lg text-black focus:ring-2 focus:ring-indigo-500 focus:outline-none"
          >
        </div>

        <div>
          <label class="block text-sm font-medium text-black mb-1">Password</label>
          <input
            required
            type="password"
            name="password"
            class="w-full px-4 py-2 border rounded-lg text-black focus:ring-2 focus:ring-indigo-500 focus:outline-none"
          >
        </div>

        <div>
          <label class="block text-sm font-medium text-black mb-1">Confirm Password</label>
          <input
            type="password"
            name="confirm_password"
            class="w-full px-4 py-2 border rounded-lg text-black focus:ring-2 focus:ring-indigo-500 focus:outline-none"
          >
        </div>

        <div>
          <label class="block text-sm font-medium text-black mb-1">Address</label>
          <input
            required
            name="address"
            class="w-full px-4 py-2 border rounded-lg text-black focus:ring-2 focus:ring-indigo-500 focus:outline-none"
          >
        </div>

        <button
          type="submit"
          class="mt-4 bg-indigo-600 text-white py-2 rounded-lg font-semibold
                hover:bg-indigo-700 transition duration-300"
        >
          Register
        </button>

      </form>
    </div>
  </div>

</body>
</html>